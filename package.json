{
  "name": "potfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio de Larissa Lima — frontend estático + backend Node/Express",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "vercel:dev": "vercel dev",
    "vercel:deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "resend": "^4.0.0",
    "validator": "^13.12.0"
  },
  "devDependencies": {
    "vercel": "^54.0.0"
  }
}
